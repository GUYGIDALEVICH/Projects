//When this button is pressed, BasePressure runs, gets reading, and then buzzer. 
//Find out how to get voltage reading



//Notes for Self, RTC needs work.
//Ok, so add the rtcBegin in this file instead of the initialization. That way, when this runs, the rtc will start from that second.
